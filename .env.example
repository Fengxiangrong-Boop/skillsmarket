# 数据库配置 (PostgreSQL)
# 本地开发使用 localhost，Docker 部署使用 db (服务名)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/skills_marketplace?schema=public"
DIRECT_URL="postgresql://postgres:postgres@localhost:5432/skills_marketplace?schema=public"

# GitHub API Token (可选，用于获取仓库信息)
# GITHUB_TOKEN="your_github_token_here"
